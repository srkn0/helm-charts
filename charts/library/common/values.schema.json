{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/bjw-s-labs/helm-charts/common-5.0.1/charts/library/common/values.schema.json",
  "type": "object",
  "properties": {
    "global": {
      "description": "Allows for configuring chart-wide settings",
      "type": "object",
      "properties": {
        "nameOverride": {
          "description": "Set the chart name",
          "type": [
            "string",
            "null"
          ]
        },
        "fullnameOverride": {
          "description": "Set the chart fullname definition",
          "type": [
            "string",
            "null"
          ]
        },
        "alwaysAppendIdentifierToResourceName": {
          "description": "Always append identifier slugs to resource names, regardless of the enabled resource count.",
          "type": "boolean",
          "default": false
        },
        "propagateGlobalMetadataToPods": {
          "description": "Set to true to propagate global metadata to Pod labels.",
          "type": "boolean",
          "default": false
        },
        "labels": {
          "description": "Set additional global labels. Helm templates can be used.",
          "$ref": "schemas/definitions.json#/$defs/labels"
        },
        "annotations": {
          "description": "Set additional global annotations. Helm templates can be used.",
          "$ref": "schemas/definitions.json#/$defs/annotations"
        },
        "createDefaultServiceAccount": {
          "description": "When true (default), automatically create a dedicated ServiceAccount using the release name as the identifier if no serviceAccount is explicitly configured. If any serviceAccount entries are present, the default is not created and user configuration takes precedence.",
          "type": "boolean",
          "default": true
        }
      }
    },
    "defaultPodOptionsStrategy": {
      "description": "Set the strategy for the default pod options. Defaults to overwrite.\noverwrite: If pod-level options are set, use those instead of the defaults.\nmerge: If pod-level options are set, merge them with the defaults.",
      "type": "string",
      "default": "overwrite",
      "enum": [
        "overwrite",
        "merge"
      ]
    },
    "defaultPodOptions": {
      "description": "Set default options for all controllers / pods here. Each of these options can be overridden on a controller level.\nUseful for setting node selectors, tolerations, security contexts, or service accounts that should apply to every pod.",
      "$ref": "schemas/pod.json#/$defs/options"
    },
    "controllers": {
      "description": "Define the Pod controllers to be generated by the chart.\nEach key is a controller identifier. Supported types: deployment (default), statefulset, daemonset, cronjob, job.",
      "type": "object",
      "additionalProperties": {
        "$ref": "schemas/controllers.json#/$defs/instance"
      },
      "examples": [
        "controllers:\n  main:\n    containers:\n      main:\n        image:\n          repository: nginx\n          tag: latest"
      ]
    },
    "serviceAccount": {
      "description": "Kubernetes serviceAccount objects to be generated by the chart",
      "type": "object",
      "additionalProperties": {
        "$ref": "schemas/serviceAccount.json#/$defs/instance"
      }
    },
    "configMaps": {
      "description": "Kubernetes ConfigMaps to be generated by the chart. Additional ConfigMaps can be added by adding a dictionary key similar to the 'config' object.",
      "type": "object",
      "additionalProperties": {
        "$ref": "schemas/configmap.json#/$defs/instance"
      }
    },
    "configMapsFromFolder": {
      "description": "Generate ConfigMaps from files in the chart filesystem",
      "type": "object",
      "properties": {
        "enabled": {
          "type": "boolean",
          "default": false
        },
        "basePath": {
          "type": "string",
          "description": "Base path containing configmap subfolders"
        },
        "autoDetectBinary": {
          "type": "boolean",
          "default": false
        },
        "configMapsOverrides": {
          "type": "object",
          "additionalProperties": {
            "type": "object",
            "properties": {
              "forceRename": {
                "type": [
                  "string",
                  "null"
                ]
              },
              "annotations": {
                "$ref": "schemas/definitions.json#/$defs/annotations"
              },
              "labels": {
                "$ref": "schemas/definitions.json#/$defs/labels"
              },
              "fileAttributeOverrides": {
                "type": "object",
                "additionalProperties": {
                  "type": "object",
                  "properties": {
                    "exclude": {
                      "type": "boolean",
                      "default": false
                    },
                    "binary": {
                      "type": "boolean",
                      "default": false
                    },
                    "escaped": {
                      "type": "boolean",
                      "default": false
                    },
                    "isEnvFile": {
                      "type": "boolean",
                      "default": false
                    }
                  },
                  "additionalProperties": false
                }
              }
            },
            "additionalProperties": false
          }
        }
      },
      "additionalProperties": false
    },
    "secrets": {
      "description": "Kubernetes Secrets to be generated by the chart. Be aware that these values are not encrypted by default, and could therefore be visible to anybody with access to the values.yaml file. Additional Secrets can be added by adding a dictionary key similar to the 'secret' object.",
      "type": "object",
      "additionalProperties": {
        "$ref": "schemas/secret.json#/$defs/instance"
      }
    },
    "secretsFromFolder": {
      "description": "Generate Secrets from files in the chart filesystem",
      "type": "object",
      "properties": {
        "enabled": {
          "type": "boolean",
          "default": false
        },
        "basePath": {
          "type": "string",
          "description": "Base path containing secret subfolders"
        },
        "overrides": {
          "type": "object",
          "additionalProperties": {
            "type": "object",
            "properties": {
              "forceRename": {
                "type": [
                  "string",
                  "null"
                ]
              },
              "annotations": {
                "$ref": "schemas/definitions.json#/$defs/annotations"
              },
              "labels": {
                "$ref": "schemas/definitions.json#/$defs/labels"
              },
              "fileAttributeOverrides": {
                "type": "object",
                "additionalProperties": {
                  "type": "object",
                  "properties": {
                    "exclude": {
                      "type": "boolean",
                      "default": false
                    },
                    "binary": {
                      "type": "boolean",
                      "default": false
                    },
                    "escaped": {
                      "type": "boolean",
                      "default": false
                    },
                    "isEnvFile": {
                      "type": "boolean",
                      "default": false
                    }
                  },
                  "additionalProperties": false
                }
              }
            },
            "additionalProperties": false
          }
        }
      },
      "additionalProperties": false
    },
    "ingress": {
      "description": "Kubernetes Ingress objects to be generated by the chart. Each key is an ingress identifier.",
      "type": "object",
      "additionalProperties": {
        "$ref": "schemas/ingress.json#/$defs/instance"
      },
      "examples": [
        "ingress:\n  main:\n    className: nginx\n    hosts:\n      - host: app.example.com\n        paths:\n          - path: /\n            pathType: Prefix\n            service:\n              identifier: main\n              port: http"
      ]
    },
    "route": {
      "description": "Kubernetes Gateway API *Route objects to be generated by the chart. Additional routes can be added by adding a dictionary key similar to the 'main' route. See https://gateway-api.sigs.k8s.io/references/spec/ for the Gateway API specification.",
      "type": "object",
      "additionalProperties": {
        "$ref": "schemas/route.json#/$defs/instance"
      }
    },
    "service": {
      "description": "Kubernetes Service objects to be generated by the chart. Each key is a service identifier.",
      "type": "object",
      "additionalProperties": {
        "$ref": "schemas/service.json#/$defs/instance"
      },
      "examples": [
        "service:\n  main:\n    controller: main\n    ports:\n      http:\n        port: 80"
      ]
    },
    "serviceMonitor": {
      "description": "serviceMonitor objects to be generated by the chart. Additional ServiceMonitors can be added by adding a dictionary key similar to the 'main' ServiceMonitor.",
      "type": "object",
      "additionalProperties": {
        "$ref": "schemas/serviceMonitor.json#/$defs/instance"
      }
    },
    "podMonitor": {
      "description": "podMonitor objects to be generated by the chart. Additional PodMonitors can be added by adding a dictionary key similar to the 'main' PodMonitor.",
      "type": "object",
      "additionalProperties": {
        "$ref": "schemas/podMonitor.json#/$defs/instance"
      }
    },
    "networkpolicies": {
      "description": "networkPolicy objects to be generated by the chart. Additional networkPolicies can be added by adding a dictionary key similar to the 'main' networkPolicy.",
      "type": "object",
      "additionalProperties": {
        "$ref": "schemas/networkpolicy.json#/$defs/instance"
      }
    },
    "persistence": {
      "description": "Configure persistent storage and mount options. Each key is an identifier for a persistence item.\nSupported types: persistentVolumeClaim (default), configMap, secret, nfs, emptyDir, hostPath, image, custom.\nBy default each item mounts to /<identifier>. Use globalMounts or advancedMounts to customize mount paths.",
      "type": "object",
      "additionalProperties": {
        "$ref": "schemas/persistence.json#/$defs/item"
      },
      "examples": [
        "persistence:\n  config:\n    type: persistentVolumeClaim\n    accessMode: ReadWriteOnce\n    size: 1Gi\n    globalMounts:\n      - path: /config\n  tmp:\n    type: emptyDir"
      ]
    },
    "rbac": {
      "description": "Configure the Roles and Role Bindings for the chart here",
      "type": "object",
      "properties": {
        "roles": {
          "description": "(Cluster)Role objects to be generated by the chart",
          "type": "object",
          "additionalProperties": {
            "$ref": "schemas/rbac.json#/$defs/role"
          }
        },
        "bindings": {
          "description": "(Cluster)RoleBinding objects to be generated by the chart",
          "type": "object",
          "additionalProperties": {
            "$ref": "schemas/rbac.json#/$defs/roleBinding"
          }
        }
      },
      "additionalProperties": false
    },
    "rawResources": {
      "description": "Allows for the inclusion of raw Kubernetes resources that are not supported by the chart otherwise​",
      "type": "object",
      "additionalProperties": {
        "$ref": "schemas/rawResource.json#/$defs/instance"
      }
    }
  }
}
